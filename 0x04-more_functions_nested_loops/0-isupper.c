#include "main.h"

/**
 * _isupper - checks for upper case character
 * @c: character to check
 *
 * Return: 1if uppercase, 0 if otherwise
 */

int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
	{
		Return (1);
	}
	else
		Return (0);
}
