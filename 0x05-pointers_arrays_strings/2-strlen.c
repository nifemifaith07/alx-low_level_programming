#include "main.h"

/**
 * _strlen - fumction that returns lenght of a string
 * @s: string
 * Return: string lenght
 */

int _strlen(char *s)
{
	int i = 0;

	if (i = 0; *s[i] != 0; i++)
		;

	return (i);
}
